# Shell example: replace with a REAL, existing folder on the mounted data drive.
# Python does not automatically load this file. Source your edited .env first.
export FUTBOL_DATA_DIR="/Volumes/YOUR_DRIVE/futbol"
# Configure this in the Ollama server environment separately if desired:
# export OLLAMA_MODELS="$FUTBOL_DATA_DIR/models/ollama"
